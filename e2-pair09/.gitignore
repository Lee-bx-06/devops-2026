# ===========================================================================
# devops-2026    A09 / B09 配对组
# 忽略规则的分工说明见 README.md 第 2 节
#
# 原则：能说明"谁产出了什么"的文件要入库，体积大或可再生的文件不入库。
# 第 24 页要求另一组能按 artifact://pair09/... 读到产物，所以 artifacts/
# 本身保持可提交，只排除其中的日志和打包文件。
# ===========================================================================

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# --- 运行日志 ---------------------------------------------------------------
# 日志体积大且可重跑，不进 Git。小产物用 JSON 报告交换。
*.log
*.out
logs/

# --- 产物目录 ---------------------------------------------------------------
# 依赖图、MD/RD 报告、Patch 要提交；日志与打包文件不提交。
artifacts/**/*.log
artifacts/**/*.out
artifacts/**/*.tar
artifacts/**/*.tar.gz
artifacts/**/*.zip

# --- 镜像与构建中间产物 -----------------------------------------------------
# Dockerfile 与镜像引用要入库，导出的镜像包不入库。
image-export/
*.tar.gz
*.tar

# --- 任务存储 ---------------------------------------------------------------
# E2 不部署服务，但本地试跑可能留下任务库。
*.db
*.sqlite
*.sqlite3

# --- 凭据与本地配置 ---------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# --- 编辑器与 IDE -----------------------------------------------------------
.vscode/
.idea/
*.swp
*~

# --- 操作系统 ---------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# --- Office 临时锁文件 ------------------------------------------------------
# 打开课件或文档时自动生成，例如 ~$E2_需求与接口契约_20260910.pptx
~$*

# --- 课程材料 ---------------------------------------------------------------
# 课件 pptx 与四篇论文 PDF 属于课程材料，不要提交到本仓库。
# 需要引用时在文档里写文件名，不要把文件本身放进来。
